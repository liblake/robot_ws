# 控制算法迁移成果归档（2026-09-09）

> 目标：把"开源 LQR+VMC 轮腿控制 → 你的串联双轮腿机器人"的当前稳定成果固化成档。
> 范围：MuJoCo 仿真（`Two_wheeled_legged_robot/robot_sim`），平地功能；真机未开始。

## 1. 运行环境与铁律

- 统一用项目虚拟环境运行，否则结果不可复现：
  ```bash
  cd ~/robot_ws/Two_wheeled_legged_robot/robot_sim
  .venv/bin/python <脚本>
  ```
- 不要用系统 `python3`（conda robot 环境 MuJoCo 版本不同；且关闭可视化窗口时
  该环境会段错误）。
- Wayland 下关窗口的 GLFW 段错误属环境问题；归档脚本在 viewer 模式结束后
  已用 `os._exit(0)` 规避。

## 2. 已验证稳定功能（平地）

| # | 功能 | 脚本 | 关键结果 |
|---|---|---|---|
| 1 | 模型加载/受控模型 | `launch_viewer.py` | 8 body / 6 执行器 / stand keyframe |
| 2 | 腿高跟踪 | `test_leg_height_tracking.py` | 稳态误差 ≤5mm，无越限 |
| 3 | 自平衡站立 | `test_stand_wheel_balance.py` | 12s：pitch≤0.046、roll=0、y 漂移≤8mm、双轮接地 |
| 4 | 前进/后退 | `test_drive.py` | ±0.5 m/s 梯形速度，稳定往返 |
| 5 | 原地转向 | `test_turn.py` | 0.3 rad/s 档稳定，正反净角 +0.23 rad |
| 6 | 原地调高 | `test_height.py` | 0.33↔0.45 m 误差 ≤1mm |
| 7 | 复合运动 | `test_composite.py` | 0.4 m/s + 0.2 rad/s 弧线稳定 |
| 8 | 10m 直线 | `test_straight_10m.py` | 26s；横漂 0、航向 0、pitch≤0.066 |
| 9 | 手柄控制 | `launch_mujoco --controller combined --scenario stand --flat-ground` | 前进/后退/转向/调高；符号与平滑已修 |

所有脚本均可加 `--viewer` 可视化（`test_straight_10m.py` 除外）。

## 3. 关键参数（当前默认，集中在这几个文件）

| 参数 | 值 | 位置 |
|---|---|---|
| 轮毂电机力矩 | ±9 N·m（实机峰值） | `mjcf_builder._replace_actuators` |
| 腿电机力矩 | ±40 N·m（软限 40） | `mjcf_builder` / `VmcParams.stand_torque_limit` |
| 腿 VMC PD | kp=200 / kd=60 | `default_params.py` |
| 轮心相对髋偏移 | -0.075 m（后移，平衡角≈1.6°） | `serial_leg_ik.wheel_y_offset` |
| 大腿 / 小腿长度 | 0.300 / 0.34325 m（URDF；实机小腿 0.34 已记录 `L2_REAL`） | `serial_leg_ik.py` |
| 腿高工作区间 | h_base ∈ [0.31, 0.50] m | `serial_leg_ik` / `mjcf_builder` cmd 滑块 |
| 腿部支撑前馈 | 解析式 `τ_j = (∂h/∂q_j)·m·g/N + Σ m_i·g·(∂z_i/∂q_j)`（2026-09-12，原 `mj_inverse`） | `vmc._analytic_support_feedforward` |
| 腿目标角速度前馈 | `stand_rate_ff_scale` = 1.0（2026-09-12 恢复上游行为，原 0） | `VmcParams` |
| 假滑块关节 | `cmd_*` 加 `armature=1e-4`（2026-09-12，修质量矩阵数值奇异） | `mjcf_builder._ensure_command_slider_joints` |
| 跳跃轨迹 | `h_min=0.32 / h_safe_high=0.48 / extend_stroke=0.16 / air_height_max=0.15`（2026-09-12 按本机重标） | `JumpTrajectoryParams` |
| 跳跃关节 PD | `jump_kp_scale=1.5 / jump_kd_scale=0.15 / jump_rate_ff_scale=1.0` | `VmcParams` |
| LAND PD | kp=80 / kd=8（2026-09-13 按质量重标，原 15/3.5 是 2.2 kg 车的标定；ζ≈0.65） | `VmcParams.kp_land/kd_land` |
| LAND 腿力矩限幅 | 40 = 执行器满量程（2026-09-13 与 STAND 的 30 拆开，落地缓冲不软限幅） | `VmcParams.land_torque_limit` |
| FLIGHT 膝位保持 | kp=80 / kd=3，锁存入 FLIGHT 瞬间膝角（2026-09-13，修空中腿部自由折叠） | `VmcParams.flight_knee_kp/kd` |
| 轮子平衡 LQR | K=[-45.0, -7.0]（2 状态；0.42m 内快恢复且长时间稳定） | `CombinedParams.wheel_balance_gain_2d` |
| 速度环比例增益 | `pitch_lean_gain` = 0.35（2026-09-11，原 0.15） | `default_params.py` |
| 位置环阻尼 | `position_kd` = 3.5（2026-09-11，原 1.5） | `default_params.py` |
| yaw 阻尼 / 积分 | 8.0 / 6.0（2026-09-11 起启用积分） | `default_params.py` |
| 转向通道限幅 | `yaw_correction_limit` = 2.0 N·m | `CombinedParams` |
| 指令斜坡限速 | 线 2.5 m/s² / 转向 1.0 rad/s²（2026-09-11，原 1.2 / 0.15） | `CombinedParams` |
| 高位限速保护 | h>0.45m 时 线速≤0.3m/s、转向≤0.2rad/s | `CombinedParams.high_height_*` |
| 手柄/滑块指令范围 | 线 ±0.8 m/s、转向 ±0.6 rad/s、高 [0.31,0.42] | `mjcf_builder` cmd 滑块 |
| 碰撞代理 | 轮子=圆柱、腿=胶囊、机身=盒 | `mjcf_builder._add_collision_proxies` |

## 4. 过程中修掉的“坑”（防止回归）

1. URDF `effort=10` 会变成关节级 `actuatorfrcrange=±10`，腿根本出不了力 → 已在 builder 中删除，由自己的 ctrlrange 管理。
2. 腿高必须用机身原点 `xpos`（不是质心 `xipos`），否则系统性偏高 11mm。
3. 简化 LQR 的轮子模型与实际单步响应差约 3.6 倍 → 2 状态经验 LQR + 实测映射。
4. 轮子方向符号与开源相反（左+1/右-1）；转向在执行器坐标中“左右同号”。
5. 球体碰撞代理会侧滚造成慢速晃动 → 改圆柱。
6. 机身前倾 13° 是质心几何造成的，不是惯量 bug → 轮心后移 75mm 调直。
7. 手柄 Y 轴方向与默认假设相反 → 映射反号；松杆阶跃会摆动 → 指令斜坡限速。
8. 爬坡失稳根因：轮速≠车身速度（爬坡/打滑时轮速虚高），外环误判“超速”
   倒拉轮子 → 速度反馈已改为机身实际前向速度。

## 5. 已知限制 / 未完成

- 速度上限：线 ±0.5 m/s、转向 ±0.3 rad/s（当前安全包线，可后续扩但需重测）。
- 高位稳定性：0.45~0.50 m 未做完整动态验证；2026-09-09 手柄实测在 0.50m +
  0.48m/s 长跑时失稳倒地，已加"高位限速联动保护"（>0.45m 自动限速 0.3m/s）；
  0.50m + 0.3m/s 松杆减速仍失稳 → 可操作上限收回 0.45m。
- 高位长期站立标定（2026-09-09）：0.42m 以下原 K[-38.2,-6.3] 120s 稳定
  （p2p≈0）；0.45m 原增益约 36s 慢发散；pitch_rate 阻尼加到 -9 虽不倒但产生
  ±0.2rad 极限环；多组增益无法在 0.45 兼顾两者 → 可操作上限设 0.42m，
  0.45+ 留待任务空间力控专项。平衡速度优化：增益调为 K[-45,-7]（扰动后
  残余晃动 p2p 0.045→0.028，0.42m 120s 稳定），站立/前进/转向/调高回归通过。
- 斜坡/roll 找平：**开发中，未完成**。已确认“地形前馈”（左右轮高差）在 3cm
  单轮坡 + 慢速配置下能把过坡 roll 峰峰从 20.2° 压到 4.6°，但结果对加减速曲线
  敏感，roll 反馈仍会失稳；需按专项继续（固定验收场景 → 遥测 → 逐层改进）。
- 斜坡专项进展（2026-09-09 后续）：速度外环改用“机身实际前向速度”后，
  右车道 2cm 坡从“摔倒倒退”改善为“能骑上坡并前进约 3m”；左车道仍失败；
  5D LQR 高度分箱通路在平地即不稳定，属移植遗留问题，暂以 2 状态经验 LQR 为
  默认。斜坡仍**未完成**，需要坡度感知目标倾角或任务空间力控级改造。
- 再尝试（2026-09-09）：增加“爬坡自适应倾角参考”（上坡时缓慢跟随地形姿态、
  出坡衰减回 0）。结果左右车道/开关注册组合互不一致（左车道无 roll 前馈时能
  前进 4.3m，右车道有前馈反而不稳），说明该启发式也不鲁棒；已保留代码但标注
  实验性，默认平地功能不受影响。
- M0 任务空间力控（2026-09-09 开始）：脚本 `test_leg_task_force.py` 已建立，
  但 Jᵀ 方向/重力前馈/执行器符号叠加未对齐，高度阶跃未通过；详见
  `SLOPE_LEG_FORCE_DESIGN.md` 第 8 节的 checkpoint。平地功能不受影响。
- 跳跃（阶段 6）：2026-09-12 跑通原地垂直起跳（第 12 节）；2026-09-13 完成
  LAND 重标 + FLIGHT 膝位保持（第 13 节）。
- 真机迁移：未开始（需电机驱动接口、IMU、主控、真实质心/尺寸）。
- 地形/波浪路/wavy road 尚未按本机轮距参数化。

## 6. 文件速查

```text
Two_wheeled_legged_robot/
├── MIGRATION_GUIDE.md        # 分阶段实施指南（0~6）
├── RESULTS_ARCHIVE.md        # 本文档
├── src/robot/robot.urdf      # 你的原始模型
└── robot_sim/                # 迁移工程（可改）
    ├── src/controllers/
    │   ├── serial_leg_ik.py  # 腿几何/IK（唯一真值源）
    │   ├── vmc.py            # 串行腿 VMC
    │   ├── combined.py       # 组合控制器（含经验轮 LQR、斜坡限速）
    │   └── default_params.py # 参数唯一入口
    ├── src/mjcf_builder.py   # URDF→MJCF + 地形/碰撞/执行器/keyframe
    └── test_*.py             # 各功能验收/演示脚本
```

## 7. 当前默认启动方式（正式入口）

```bash
cd ~/robot_ws/Two_wheeled_legged_robot/robot_sim
.venv/bin/python -m src.launch_mujoco --controller combined --scenario stand --flat-ground
```

## 8. 平地驾驶性能专项（2026-09-11）

目标：把平地加减速 / 刹车 / 转向 / 速度拉到开源对照车
（`wheel_legged_robot_sim-main`，2.2 kg 四连杆并联腿）的水平。

### 8.1 量化差距（无头对照，同一速度曲线，前进方向均为 +Y）

| 指标（0.5 m/s 梯形，1.0 m/s² 加减速） | 开源对照 | 本机（改动前） | 本机（改动后） |
|---|---|---|---|
| 匀速段速度跟踪 rms | 0.038 m/s | 0.126 m/s | **0.015 m/s** |
| 匀速段 \|误差\|max | 0.098 m/s | 0.267 m/s | **0.046 m/s** |
| 0 → 90% 指令加速时间 | 0.88 s | 1.01 s | **0.80 s** |
| 松杆刹车时间（从 0.5 m/s） | 0.75 s | 2.07 s | **0.83 s** |
| 松杆刹车距离 | 266 mm | 489 mm | **279 mm** |
| 转向 cmd 0.5 rad/s 实测 | 0.500 | 0.404 | **0.500** |
| 转向 0 → 90% 上升时间 | — | 3.23 s | **1.20 s** |

### 8.2 根因

1. **速度环比例增益太低**：`pitch_lean_gain` 是"速度误差 → 目标前倾角"的比例项，
   0.15 对应速度环带宽只有 ~1.5 rad/s。加减速完全靠积分慢慢攒，松杆后刹车
   属于"指数衰减"（误差越小倾角越小），尾巴很长。
2. **转向只有 P 阻尼、没有积分**：轮地摩擦需要力矩才能维持转速，纯 P 必然留
   19% 稳态速差（cmd 0.5 → 实际 0.40）。
3. **指令斜坡限速过保守**：线 1.2 m/s²、转向 0.15 rad/s²，后者让"0 → 0.5 rad/s"
   要 3.3 s，手感发钝。
4. **停车积分门控用的是布尔开关**：外部指令刚跨过 0 的那个周期，积分门控从 1
   直接跳到 `smoothstep(|v|/release_speed)`，在目标倾角上留下一次阶跃；
   2 状态轮 LQR 的 K[0]=45 会把它放大成 `45·Δθ` 的轮力矩跳变。
   增益调到 0.35 后这个跳变从 0.05 涨到 0.45 N·m（旧版本被用户感知为"顿挫"）。
5. **转向通道没有输出上限**：`yaw_damping=8` 在机身被外力偏航时可以瞬间输出
   顶到轮子 ±9 N·m 峰值的差动力矩，把本该留给平衡的量程吃光。

### 8.3 改动

| 文件 | 改动 |
|---|---|
| `default_params.py` | `pitch_lean_gain` 0.15→0.35；`position_kd` 1.5→3.5；`yaw_ki` 0→6.0 |
| `combined.py` | `max_linear_accel` 1.2→2.5；`max_yaw_accel` 0.15→1.0；新增 `yaw_correction_limit`=2.0 / `yaw_integral_limit`=1.0；停车积分门控改为连续量 `_zero_request_blend`；`_compute_yaw_correction` 改条件积分 + 输出限幅 |
| `mjcf_builder.py` | cmd 滑块范围：线 ±0.5→±0.8 m/s、转向 ±0.3→±0.6 rad/s |
| `default_params.py` | 上述新字段补进 `params_to_dict` / `params_from_dict`（round-trip 一致） |

### 8.4 改动后回归

`test_stop_response` PASS（轮力矩跳变 0.035 N·m，反向位移 8.2 mm，0.90 s 停稳）；
`test_drive` / `test_turn` / `test_height` / `test_composite` / `test_straight_10m`
全部 PASS；`test_slope_v2` 过 2cm 单轮坡 \|pitch\|max 0.146→**0.073**、
单轮离地 4/159→**0/159**（速度环变强后爬坡余量反而变大）；
`test_stand_wheel_balance` 12s \|pitch\|max 0.047。

包线复测（默认参数，无摔倒）：0.5 / 0.8 / 1.0 m/s 跟踪误差 ≤0.001 m/s；
0.8 m/s + 0.6 rad/s 复合、0.8 rad/s 原地转，在 0.37 / 0.42 m 站高下均通过；
轮力矩峰值仅 1.2~2.6 N·m（限幅 9）。

### 8.5 仍未解决

- `test_static_wheel_pad.py`（静态单轮垫高 1cm）仍 **FAIL**：改动前就 FAIL
  （离地 800/2500 步、\|roll\| 6.6°），改动后改善到 153/2500、1.2°，
  但 5% 阈值仍差一点。属第 5 节"斜坡/roll 找平未完成"同一条线。
- 转向仍靠经验阻尼 + 积分，没有像开源车那样走"转向通道进 LQR"的正式结构。

## 9. 腿层支撑前馈改为解析式（2026-09-12）

目标：把 VMC 里"撑住整车"的前馈从 `mj_inverse`（qacc=0 接触逆动力学）换成
不依赖仿真器内部量的解析式，对齐开源车思路并为实机做准备。

### 9.1 公式与分解

```text
τ_j = (∂h/∂q_j)·(m_total·g)/N_legs  +  Σ_{i∈腿链} m_i·g·(∂z_i/∂q_j)
       └─ 整车重量经轮子传导的载荷路径 ─┘   └─ 腿连杆自重（标准关节重力矩）─┘
```

第一项就是开源车 `vmc.py` 的 `jacobian[side]·total_mass·gravity/N` 等价形式
（虚功：轮心竖直支承力 F=m·g/N 对应 τ_j=F·∂h/∂q_j）。
第二项开源车可以省（它四连杆空心杆很轻），本机不能省：腿链 3.5 kg/腿 =
总质量的 21%，省掉在膝上差 5.5 N·m、髋上差 3.6 N·m，仿真实测静立腿高
会偏 7.2 mm、pitch 偏 0.0053 rad。

### 9.2 验证（`test_support_ff.py`，新增）

静立姿态下解析式与旧 `mj_inverse` 一致（0.33/0.37/0.42/0.46 m 四个高度，
最大偏差 0.02 N·m / 0.17%）；"撑住整车"判据（把腿电机力矩设成该前馈后腿
关节残余角加速度）解析式 0.00~0.02 rad/s²、零前馈 37~42 rad/s²。

同时发现旧方案的一个隐患：`mj_inverse` 有速度相关项、且会复用上一步的接触力，
在**旋转**这类动态工况下给出错误值——

| 工况 | `mj_inverse` 前馈 | 解析前馈 | 零前馈 |
|---|---|---|---|
| 站立 | 0.00 | 0.00 | 38.39 |
| 直行 0.6 m/s | 0.05 | 0.07 | 38.39 |
| 原地转 0.8 rad/s | **133.60** | **5.86** | 36.69 |

（数值 = 撑住整车残余 \|qacc\| rad/s²，越小越好。原地转时旧方案比"不给前馈"还差。）

### 9.3 改动后效果

平地驾驶逐项不变（0.5 m/s：跟踪 rms 0.015、刹车 279→283 mm、加速 0.80→0.81 s）；
转向 0.6 rad/s 稳态速差不变（0.600/0.599），roll 0.02°→0.34°（仍可忽略）。

**过障明显变好**：同一个 2 cm 单轮梯形坡 + 0.3 m/s，旧方案在 t=10.25 s 翻车
（\|pitch\| 178.6°，三次复现一致），新方案全程不翻（\|pitch\|max 11.4°、
过坡段 roll 峰峰 2.6°→0.8°）。原来记在"斜坡未完成"里的一次翻车其实是
不可靠前馈引起的。

整条过障包线（左轮单侧梯形坡，均不翻车）：

| 坡高 | 0.20 m/s roll 均值/峰峰 | 0.30 m/s | 0.45 m/s |
|---|---|---|---|
| 20 mm | 0.06° / 0.28° | 0.15° / 0.76° | 0.34° / 1.77° |
| 40 mm | 0.29° / 1.69° | 0.75° / 4.19° | 1.20° / 5.73° |
| 65 mm | 0.93° / 6.37° | 1.91° / 9.31° | 2.40° / 10.97° |

对照开源车同一条 65 mm 坡：roll 均值 0.54~0.85°、峰峰 2.4~3.8°。
仍差 2~3 倍，但原因是车本身：半轮距/质心高（抗侧翻余量）本机 0.76、
开源车 1.76——本机 37° 就翻、开源车要 60°，天生对 roll 敏感 2.3 倍。

**波浪路（washboard，y∈[1.05,2.25]，仅波浪路、已移除梯形坡）**：

| 车速 | 旧方案 `mj_inverse` | 新方案解析式 |
|---|---|---|
| 0.30 m/s | 通过，pitch 23.3° / roll 13.7° / 离地 40.3% / z 加速度 rms 21.6 | 通过，pitch 11.5° / roll 0.0° / 离地 0.3% / 2.5 |
| 0.50 m/s | **翻车**（pitch 46.1° / roll 19.9°） | 通过，pitch 7.1° / roll 0.1° / 离地 0.9% / 3.9 |
| 0.80 m/s | 通过，pitch 21.3° / roll 12.1° / 离地 42.6% / 17.9 | 通过，pitch 5.8° / roll 0.1° / 离地 9.7% / 8.8 |
| 1.00 m/s | **翻车**（pitch 45.8° / roll 13.5°） | 通过，pitch 8.5° / roll 0.2° / 离地 22.4% / 12.4 |

（z 加速度 rms 单位 m/s²，是"地形隔离度/舒适性"指标。）
波浪路上差距尤其大：旧方案前馈在有速度时本来就不可信，路面的高频冲击
把它的错误值进一步放大，导致离地 40%+、侧倾 12~20°、两个速度翻车。

> 注：`test_wavy_road.py`（2026-09-12 新增）目前**没有传 `terrain=`**，
> `prepare_controlled_mujoco_xml` 在 `terrain=None` 时不加任何地形，
> 所以它跑的是平地，表里的 roll/air%/sat% 全是 ~0 是假象。
> 上表用"传入 terrain 后再删掉梯形坡 geom"的独立脚本测得。

### 9.4 仍未解决

- 解析式只补重力（含连杆自重），不含速度相关项（Coriolis/离心）。
  实测原地转 0.8 rad/s 时残余 \|qacc\| 5.86 rad/s²，对应腿高误差约 2 mm
  （旧方案 1.25 mm）。腿关节速度本来很低，暂不补；要补时把
  `Σ m_i·g·(∂z_i/∂q_j)` 换成 RNEA 的完整 bias 项即可。

## 10. 地形跟随专项（2026-09-12，接第 9 节）

目标是"把 `roll_level_offset_limit` 那套手搭前馈升级成地形感知的目标倾角参考"。
结论：**真正的杠杆不在倾角参考，而在腿的目标角速度前馈**——把它恢复成 1.0
之后，本机过障/波浪路的指标全面超过开源对照车。

### 10.1 先排除的路（都试过，收益不成立）

| 试的东西 | 现象 | 结论 |
|---|---|---|
| 找平前馈上限 0.02/0.035/0.06/0.10 | roll 峰峰 9.02/9.31/9.31/9.31° | 0.035 已饱和，加限幅无用 |
| 找平 roll 反馈 `roll_level_kp_height` 0.5/1.0/2.0 | roll 峰峰 21.5/21.8/10.9° | 明显变差，维持 0 |
| 找平 roll 阻尼 `roll_level_kd_height` 0.02/0.05 | roll 峰峰 11.6/15.8° | 变差，维持 0.002 |
| 轮子离地时冻结找平估计 | 离地 26→28 步、roll 9.31→8.96° | 无意义 |
| 地形坡度观测器 + 提前刹车倾角 | k=0.5 时 v 冲高 0.303→0.288；k≥0.8 时 pitch 飙到 22~25° | 弱且脆，不采纳 |

还查清了一件容易误判的事：**车身 roll 近似等于 (轮高差 + 腿指令高差)/轮距**
（由虚功/小角度运动学），所以"残余 roll"本质是**腿没跟上指令**，不是姿态参考
没设对。65 mm 坡 @0.3 m/s 时腿的跟随误差只有 4~6 mm，对应 roll 1~1.6°，
与实测 2.7° 同量级——所以该修的是腿的跟随速度。

### 10.2 真正的杠杆：恢复腿的"目标角速度前馈"

上游 VMC 的关节 PD 是
`τ = kp·(θ_target − θ) + kd·(θ̇_target − θ̇)`，其中 `θ̇_target` 由目标腿高的
变化率经雅可比换算而来——它让腿**主动去追**移动中的地形目标。
本机 2026-09-11 把它整个置 0（当时的目标角速率 0.5~0.94 rad/s 会让
`kd·rate` 单独打满 ±30 N·m 软限幅，过障翻车）。

复测发现当时的真因是**支撑前馈用 mj_inverse**（动态下给出错误值，见第 9 节），
不是这条前馈。第 9 节换成解析式之后，恢复它反而变成主要收益来源：

| 场景（过坡段） | `stand_rate_ff_scale`=0 | =0.8 | =1.0（现默认） |
|---|---|---|---|
| 65 mm 坡 @0.3 roll 峰峰 | 9.31° | 0.65° | **0.30°** |
| 65 mm 坡 @0.45 | 10.97°，离地 4.6% | 1.21° | **0.27°，离地 0%** |
| 40 mm 坡 @0.45 | 5.73° | 0.41° | **0.14°** |
| 20 mm 坡 @0.3 | 0.76° | 0.06° | **0.07°** |
| 波浪路 @0.8 离地占比 | 9.4% | 6.5% | **6.6%** |
| 波浪路 @1.0 离地占比 | 20.4% | 18.6% | **18.9%** |
| 平直 0.8 m/s | 逐位不变 | 逐位不变 | 逐位不变 |

腿电机饱和占比没有变差（65 mm @0.45：4.0% → 0.4%；波浪路 @1.0：15.9% → 15.5%）。

新增参数 `VmcParams.stand_rate_ff_scale`（默认 1.0 = 上游行为，0 = 旧行为），
已进 `params_to_dict / params_from_dict`。

### 10.3 与开源对照车的同口径对比

同一套测试脚本、同一条左轮单侧梯形坡：

| 场景 | 开源对照车 | 本机（改后） |
|---|---|---|
| 65 mm 坡 @0.20 roll 峰峰 | 2.37° | **0.37°** |
| 65 mm 坡 @0.30 roll 峰峰 | 2.82° | **0.30°** |
| 65 mm 坡 @0.45 roll 峰峰 | 3.76° | **0.27°** |
| 波浪路 @0.5 离地占比 | 15.8% | **0.7%** |
| 波浪路 @0.8 离地占比 | 29.4% | **6.6%** |
| 波浪路 @1.0 离地占比 | 34.8% | **18.9%** |

过障/波浪路已经**反超**开源车；之前归因为"本机抗侧翻余量只有 0.76、天生敏感
2.3 倍"的差距，改完之后不再成立——那部分其实是腿跟随滞后。

### 10.4 回归

12 个脚本全 PASS，且多项变好：

| 脚本 | 改前 | 改后 |
|---|---|---|
| `test_slope_v2` 过坡 roll 峰峰 | 9.31° | **0.1°** |
| `test_stop_response` 轮力矩跳变 | 0.035 / 0.035 N·m | **0.018 / 0.018** |
| `test_stop_response` 腿力矩跳变 | 0.484 / 0.442 N·m | **0.093 / 0.088** |
| `test_static_wheel_pad` max_roll | 0.14° | **0.00°** |
| `test_leg_height_tracking` 稳态误差 | +0.1 / +0.9 / −0.9 mm | +0.2 / +1.1 / −1.0 mm |

### 10.5 仍未解决

- 波浪路 1.0 m/s 仍有 18.9% 离地、腿电机饱和 15.5%。再往上要么提高腿电机
  软限幅/峰值力矩，要么上任务空间力控（阻抗控制）让腿对地面冲击更"软"。
- 过障时 pitch 峰值仍有 ~11.5°（65 mm 坡）。这部分是平衡环对抗地形扰动的
  比例稳态误差，属于"结构性问题"，不是找平能解决的；要压下去需要专门的
  坡度扰动前馈力矩（第 10.1 节试过的倾角参考方向已证明无效）。

## 11. 鲁棒性体检（2026-09-12）

目标：现有结论都来自固定标称场景，不知道离边界多远。脚本：
`test_robustness.py`（4 个场景 × 25 种扰动）。

### 11.1 结论一览

| 维度 | 范围 | 结论 |
|---|---|---|
| 质量 | 全机 ×0.70~×1.30 | 全部 OK |
| 质量 | 只改机身 ×0.70 / ×1.30 | 全部 OK |
| 质心 | 机身前后 ±5 cm | 前移 OK；**后移 5 cm 时行驶 pitch 0.282 > 0.25（WARN）** |
| **控制回路延迟** | **≤5 ms** | **全部 OK** |
| | 10 ms | 4 个场景中 3 个 WARN |
| | **≥20 ms** | **4 个场景全部摔倒** |
| 传感 | IMU 姿态噪声 ≤1.0°、编码器 12bit 量化 | 全部 OK |
| 地面 | 摩擦 ×0.5 / ×2 | 全部 OK |
| 增益 | 平衡增益 ±20%、速度环增益 ±20% | 全部 OK（调参面不尖） |
| 扰动 | 站立时 ±15% 自重水平推力 | **stand WARN：pitch 0.280、y 漂移 0.371 m** |
| 组合 | 质量×1.15 + 20 ms + 传感 | 全部摔倒（延迟主导） |

**唯一硬伤是控制回路延迟**：

| 延迟 | 4 ms | 6 ms | 8 ms | 10 ms | 12 ms | 16 ms | 20 ms |
|---|---|---|---|---|---|---|---|
| stand \|pitch\|max | 0.031 | 0.045 | 0.164 | 0.264 | 0.431 | **摔倒** | **摔倒** |

即：**总延迟 ≤6 ms 完全干净，8~14 ms 退化但还能撑，≥16 ms 直接摔**。
对真机的含义：从 IMU 采样 → 控制解算 → 力矩下发（含电流环）必须压进 ~6 ms。
500 Hz 控制周期本身只有 2 ms，所以这个预算不算苛刻，但必须有意识地去量。

### 11.2 过程中修掉的一个模型地雷

做质量扫描时发现：质量 ×0.85 / ×1.10 / ×1.15 / ×1.20 会让 `mj_forward`
直接返回 NaN（×0.70 / ×0.90 / ×1.30 却正常）。根因是 `mjcf_builder` 造出来的
4 个 `cmd_*` 假滑块关节挂在质量为 0 的 `command_slider_body` 上，又没有给
`armature`，它们的质量矩阵对角只有 MuJoCo 的下限 1e-9 → 整机质量矩阵最小
特征值 ~1e-26，**数值奇异**；改质量后 roundoff 翻符号就崩。

修法：给这 4 个关节加 `armature="1e-4"`（第 11.3 节）。它们只承载手柄/滑块
指令、不带约束也不出力，对机器人动力学零影响——12 个回归脚本数值逐位不变。

### 11.3 体检脚本自身的两个坑（记下来别再踩）

1. **不要在运行时改 `model.body_mass/inertia` 再 `mj_setConst`**：实测对某些
   缩放系数会算出 NaN 模型。改为直接改 XML 的 `<inertial>` 再重新加载。
2. **不要把 IMU 噪声写进 `data` 再 `mj_forward`**：`mj_forward` 会留下按扰动
   状态解出的接触力/热启动项，污染真值，实测让机器人凭空产生 13~15° 稳态倾角。
   正确做法是只改传给控制器的 state 副本。改对之后同一组噪声下
   pitch 标准差只有 0.03°，也就是**控制器对状态噪声几乎免疫**。

### 11.4 体检本身的局限

- 控制器有若干处直接读 MuJoCo 内部量：`data.xmat`(5 处)、`data.xpos/xipos`(8 处)、
  `data.qpos/qvel`(8 处)、以及 **`data.ncon` 接触数**。本体检只对"姿态 + 关节
  编码器"两类做了噪声/量化注入；接触数仍给真值。真机上接触检测本身就是个
  未实现的模块（支撑前馈 `ncon>=2`、站立平衡 `contact_count>=2`、起跳判定
  `ncon==0` 都依赖它）。
- 延迟按"控制器输出滞后 N 步"建模，未含通信抖动、电流环动态、采样抖动。
- 场景只有 4 个（站立/行驶/转向/过障），未覆盖波浪路与高位站姿。

### 11.5 由此得到的结论

1. **需要优先解决的是延迟预算**，不是增益鲁棒性——增益、质量、质心、摩擦、
   传感噪声的余量都很足。
2. **参数标定的可移植性没问题**：±20% 增益偏差、±30% 质量都不影响通过，
   说明换到真机后即使质量/几何有偏差，也不会因为"调参太尖"而失稳。
3. **站立抗持续外力的能力偏弱**（±15% 自重推力 → 漂 0.37 m）。这是设计取向
   （位置环 `position_kp=3` / `position_velocity_limit=0.6` 本身偏软），
   如果实机上需要在坡上/顶风保持站位，需要专门加强。

## 12. 跳跃（阶段 6 第一步，2026-09-12）

目标：把跳跃跑通（原地垂直起跳，看顶点高度 + 落地不摔）。相位机 /
轨迹类 / 相位独占调度都是开源工程整份搬过来的，本次是"重标参数 + 补一处算法"。

### 12.1 起点：按跳跃键等于没跳

原来触发一次满幅跳跃，机身只动 **5.2 mm**。原因是 `JumpTrajectoryParams`
整套还是开源车的 LUT 数值（h_min=0.0785 / h_safe_high=0.140）：
本机 h_start=0.37 代进去得到 h_low=0.095、h_high=0.140，再被 IK
`clamp_height([0.31,0.50])` 夹住 → **两端都变 0.31、行程为 0**。

### 12.2 缺的那一处算法：动态推力前馈

上游 EXTEND 用 `τ = (dh/dθ)·m·(g + ḧ_target)/N` 把整机蹬起来。本机
`target_h_ddot` 在 vmc.py 里算了 3 处但**一次都没被使用**——没有任何
`m·(g+ḧ)` 项（第 9 节把 `mj_inverse` 换成解析式支撑前馈时，那条推力项本来
就不存在）。结果 EXTEND 只有位置 PD 在推，跟不上 0.1~0.2 s 级的伸腿。

补法：给 `_analytic_support_feedforward` 加 `vertical_accel` 形参，把 g 换成
`g + ḧ`（两项一起缩放），在 CROUCH/EXTEND/LAND 每步现算，且**不按接触门控**
（EXTEND 蹬到最后会瞬间离地，正是最需要推力的时候）。`ḧ=0` 时与静态版逐位
相同 → STAND 行为零影响。

### 12.3 重标参数

| 参数 | 原值（开源车 LUT） | 本机 |
|---|---|---|
| `h_min` | 0.0785 | **0.32**（IK 下限 0.31 + 10 mm 余量） |
| `h_safe_high` | 0.140 | **0.48**（IK 上限 0.50 + 20 mm 余量） |
| `extend_stroke` | 0.045 | **0.16**（0.32→0.48 用满区间） |
| `air_height_max` | 0.40 | **0.15**（见 12.5 的实测标定） |
| `land_duration` | 0.25 | 0.30 |
| `h_stand_after_land` | 0.142 | 0.37 |
| `crouch_depth` | 0.05 | 0.05（不变） |

### 12.4 过程中修掉的自激：kd 在跳跃相位必须压小

补完推力前馈后能跳了，但 EXTEND 期间**四条腿电机全程顶在 ±40 N·m 饱和上**。
查出来是髋关节的 D 项自激：

| | EXTEND 髋角速度 | D 项 | 结果 |
|---|---|---|---|
| kd=90（60×1.5） | 逐拍交替 ±0.5 rad/s（98 步 89 次变号） | ±65 N·m | 抖振吃掉能量，**完全跳不起来** |
| kd=9（60×0.15） | 平滑 | — | EXTEND 峰 36 N·m（不饱和），弹道反而最高 |

原因：本机的 D 项被当"速度前馈"用（`kd·(θ̇_target − θ̇)`），增益大时与
"关节—地面接触"这条刚度环形成 2 步极限环。kp 不用动（位置跟踪靠推力前馈 +
比例项）。新增 `jump_kp_scale=1.5 / jump_kd_scale=0.15 / jump_rate_ff_scale=1.0`。

速度前馈本身是必要的：本机每腿 2 自由度，"轮心保持在髋正下方"的站姿律让髋角
在伸腿过程中摆动 ≈0.47 rad，这段姿态运动不在腿高方向、推力前馈管不到。
实测关掉它弹道从 60.9 mm 掉到 44.9 mm。

### 12.5 实测（`test_jump.py`，新增）

| 幅度 | 弹道升高（质心） | 质心总升 | EXTEND 腿力矩峰 | 落地后 \|pitch\|max | 摔 |
|---|---|---|---|---|---|
| 0.4 | 21.0 mm | 98.5 mm | 23.2 N·m | 1.9° | 否 |
| 0.7 | 38.2 mm | 118.2 mm | 32.3 N·m | 4.3° | 否 |
| 1.0 | **60.9 mm** | 141.4 mm | **36.4 N·m** | 8.2° | 否 |

起始站高 0.33 / 0.37 / 0.42 / 0.46 m 都能跳，弹道稳定在 60.8~60.9 mm
（固定 `extend_stroke` 让窗口整体平移，正是上游那个设计意图）。

两个容易踩的测量坑（脚本里注释了）：
- **跳跃高度必须用质心，不是机身原点**。腾空时伸腿会把机身推高但质心不动。
- 轨迹的 `v_target` 是**腿高变化率**，质心只跟约 2/3，所以
  **实测弹道 ≈ `air_height_max` × 0.4**。要更高的跳跃得按这个比例反推。

### 12.6 顺带拿到的一个数：腿电机力矩需求

跳 61 mm 需要 EXTEND 腿力矩峰 **36.4 N·m**（双膝限制，`dh/dq≈0.20~0.22`）。
这比"站在地上"需要的 ~12 N·m 高 3 倍，是目前唯一能把腿电机规格逼出来的场景。
仿真里腿执行器上限是 ±40（探索值），本次结果刚好留了 9% 余量。

### 12.7 仍未做（2026-09-13 更新：前两项已完成，见第 13 节）

- ~~**FLIGHT 空中姿态阻尼只驱动髋**~~ → 已做膝位保持（`flight_knee_kp/kd`），
  姿态反作用力偶仍由髋独自承担（膝被位置保持占用），实测够用。
- ~~**LAND 增益还是上游值**~~ → 已按质量重标 `kp_land=80 / kd_land=8`。
- **EXTEND 期间的 pitch 角动量注入没有前馈抵消**。本次原地跳 pitch 峰值 8°，
  可接受；但上游记录过"低站高起跳前倾明显更大"，本机在不同 h_start 下
  落地 pitch 6.4~8.9° 还算一致。实测加大 `flight_pitch_kd`（1.5→4.5）对飞行
  pitch 无改善——峰值在离地瞬间就被 EXTEND 反作用决定，0.3 s 飞行内 ±3.5 N·m
  的髋阻尼改变不了弧线，根治必须做起跳反作用前馈。
- 跳跃时**横向漂移/航向**没做验收（原地跳几乎不产生）。

## 13. 落地吸收 + 空中姿态（2026-09-13）

目标：解决 12.7 的前两项（LAND 增益按质量重标、FLIGHT 膝自由），并搞清楚
"跳跃高度还能不能往上推"。

### 13.1 探针发现的两个隐藏事实

无头探针（/tmp/probe_land.py 思路：量化触地速度/缓冲行程/力矩饱和/抖振/回弹）
对比新旧参数的落地轨迹，发现：

1. **空中腿部自由折叠**（旧行为）：膝在 FLIGHT 力矩恒 0，腿在重力 + 髋姿态
   阻尼反作用之下折叠 ~60 mm，轮子提前收拢触地——触地时质心几乎还在弹道
   顶点。后果：落地冲击被"腿折叠行程"吃掉、LAND 面对的是低速度 + 过度折叠
   的腿，落地初始姿态（离伸直还有多少缓冲余量）看运气。修复：FLIGHT 膝
   小刚度 PD 锁在入 FLIGHT 瞬间的膝角（= 起跳蹬直位），髋继续独占姿态阻尼。
2. **test_jump 的弹道测量低估 ~6 mm**：`last_contact` 按 `t <= takeoff_time`
   取，把首个 flight 样本（其实已离地、正在爬升）当成了接触点。已改为
   "时间上不晚于首个 flight 样本的最后一个真接触样本"（样本新增
   contact_count 字段）。旧归档数字 21.0/38.2/60.9 = 两个误差（测量 −6、
   折叠导致的触地推高 +6~7）恰好抵消的产物；修正后真实弹道
   **23.1 / 41.4 / 62.5 mm**（幅度 0.4/0.7/1.0），与物理预测吻合
   （v_CoM≈0.63×v_target，1.09²/2g = 60.5 mm）。

### 13.2 LAND 重标

kp_land 15→**80**、kd_land 3.5→**8**。上游 15/3.5 是 2.2 kg 的标定；本机
17 kg。任务空间等效 k=kp/J²（J²≈0.055）、c=kd/J²，对每腿 8.5 kg 的缓冲模态
ω_n≈13 rad/s：kd=8 → ζ≈0.65（kd=3.5 → 0.28 欠阻尼；kd=12 → 0.97 过阻尼且
触地步膝力矩抖振 4~8/150）。落地 0.6 s 内指标（air=0.15/0.18）：

| | 旧（15/3.5，无膝保持） | 新（80/8 + 膝保持） |
|---|---|---|
| 恢复时间 | 0.61 / 0.59 s | **0.44 / 0.40 s** |
| 膝力矩变号（抖振） | 4/150 | **0/150** |
| 落地 pitch 峰 | 8.2° / 12.4° | **8.1° / 9.1°** |
| 触地速度（真实弹道弧） | ~0（提前触地） | 1.09 / 1.20 m/s（完整弧） |
| 回弹 | 0.09~0.10 m/s 向上 | **无（−0.02~−0.06）** |

`land_torque_limit` 30→**40**（= 执行器满量程）：LAND 从"与 STAND 共用 30"
拆出来，落地缓冲不做额外软限幅（动态推力前馈承担大头，PD 只补跟踪误差，
实测缓冲环节峰值 ~40 N·m 出现在触地步，30 会把缓冲变形顶死）。

### 13.3 跳跃高度天花板（结论：维持 0.15，极限 ~0.18）

膝位保持 + 新 LAND 之后，落地不再是瓶颈——**air_height_max=0.20 时落地
依然干净**（弹道 81.7 mm、恢复 0.45 s、pitch 12.7°、无抖振）。瓶颈只剩
EXTEND 推力：0.15 → 36.4 N·m（9% 余量）；0.16 → 38.1（5%）；0.18 → 38.1；
0.20 → 顶满 40（10 步饱和）。`air_height_max` 维持 0.15 不动；要跳更高必须
先提高腿电机规格（仿真 ctrlrange ±40 是探索值）或加起跳反作用前馈抑制
pitch 注入。

### 13.4 回归

`test_jump` PASS（23.1/41.4/62.5 mm，落地 pitch 5.1/7.2/8.1°）；
`test_slope_v2` PASS（roll 峰峰 0.1°、单轮离地 0/159，与改动前一致）；
`test_stand_wheel_balance` PASS（|pitch|max 0.046）；`test_composite` PASS。
参数 round-trip（`params_to_json`/`params_from_json`）一致。

## 14. 连跳偏航失控复盘 + 跳跃触发门槛（2026-09-13 下午）

现象（用户交互实测 + 录屏）：连按跳跃时第二次跳起飞后机身打转，落地后
"为了平衡开始转弯"，连跳 4 次后彻底乱套（累计 −147°，偏航速率 ±400°/s）。

### 14.1 遥测复盘结论（logs/manual/run_20260913_140825）

逐帧分析 813k 行遥测 + 无头复现，定位到三个叠加因素：

1. **第一次跳跃本身是对称且干净的**（腿力矩 L/R 15.7/15.7、30.0/30.0；
   落地 yaw 仅 +3.2°）。"两条腿幅度不一样"是旋转/打滑落地时两腿力矩差
   （50.1 vs 22.1 N·m）的视觉表现，不是控制 bias。
2. **灾难的直接引信是"落地后 1.06 s 就再跳"**：落地回 STAND 后俯仰还在
   ±4° 振荡、轮子带残余转速，第二次跳的下蹲阶段轮子载荷波动（接触数
   4→2 闪断）+ 等力矩打滑不均 → 偏航角速率 −7°/s 一路涨到离地 −195°/s；
   空中无偏航阻尼（角动量守恒），落地 −147° 且单轮先着地继续转。
3. **落地后的新朝向被锁成航向基准**：`_handle_stand_entry` 在 STAND 进入
   时锁当前航向为 anchor，落地蹭掉的角度永远不回正，逐跳累积。

另发现遥测 `roll` 列与 `pitch` 列全程互为相反数——本机前进轴是 +Y，
`state.py` 的欧拉角提取存在轴间串扰（真实侧倾≈0，读数是伪像）。
**待修**：修 `_roll_from_quaternion` 的提取公式（不影响控制，控制用的
roll_rate 来自陀螺仪角速度，无此问题）。

### 14.2 修复

| 修复 | 位置 | 内容 |
|---|---|---|
| 跳跃触发稳定门槛 | `launch_mujoco._trigger_jump_on_rising_edge` | STAND 停留 ≥1.2 s + 双轮接地 + \|pitch\|<0.2 rad + 俯仰/侧倾/偏航角速率、轮残余转速各自限值；不满足则丢弃本次按键（不排队，需松开重按），控制台打印原因 |
| yaw 积分冻结 | `combined._compute_yaw_correction(accumulate=False)` | 跳跃地面相位（CROUCH/EXTEND/LAND）不再累积 yaw 积分——弹跳/打滑期误差单边，会把积分顶满上限带进下一阶段 |
| 落地航向恢复 | `combined._handle_jump_entry/_handle_stand_entry` | 起跳前锁存航向锚，落地回 STAND 时恢复它；heading_hold 以 0.8 rad/s 限幅把落地蹭掉的角度温柔拉回 |
| yaw 限幅实验（否决） | `CombinedParams.jump_yaw_correction_limit` | 曾把跳跃相位 yaw 限幅放宽到 5 N·m 想硬压螺旋，实测落地弹跳期 yaw 速率信号是冲击噪声，±5 N·m 差动把轮子甩到对转（峰值 286°/s）反而更糟——默认收回 2.0，参数保留供实验 |

### 14.3 验证

- 触发门槛三场景：开机 0.5 s 触发被拒 ✓；静立 2 s 触发允许 ✓；空中按键忽略 ✓。
- 无头单跳：最终 yaw **−0.0°**（原 +3.3° 逐跳累积），峰值 yaw 速率 36°/s（飞行段，正常）。
- 无头"落地后 1.06 s 急连跳"（绕过门槛直接 start_jump，模拟最坏情况）：
  第二跳仍有瞬态打滑尖峰，但落地稳定后 yaw 仅 **+0.7°**（交互事故里是 −147°）。
- 回归：`test_jump` PASS（23.1/41.4/62.5 mm 不变）、`test_slope_v2`、
  `test_stand_wheel_balance`、`test_turn` 全部与改动前一致；round-trip 一致。
- 交互使用注意：跳跃键按下后如果被门槛拒绝，等 ~1.5 s 松开重按即可。

## 15. 跳跃偏航总根源：修正符号漏取反（2026-09-13 傍晚）

用户复测（录屏 15-26）仍见跳跃大幅旋转。遥测（run_20260913_152638）显示：
第 1 跳干净（过程 yaw 峰 4.2°、落稳回 0、漂移 0.5 cm），第 2/3 跳单跳净偏
+78°/−94°（过程峰值 153°/−176°）——且触发时所有静稳指标正常、门槛零拒绝。

### 15.1 根因：跳跃相位的 yaw 修正是正反馈

决定性实验：站立时机体上注入饱和轮力矩对（−2/−2 N·m，正是 yaw 修正饱和
时的输出形态）→ 0.5 s 机体自转 **+103°**，与偏航速率同号。

机理：本机左轮几乎在偏航轴上（x≈−0.01）、右轮在 x=+0.35，转向力矩几乎
全部来自右轮；`_compute_yaw_correction` 的符号沿用了开源"同向轮轴"约定，
而本机轮轴相反。`_stand_control` 的 2 状态路径 2026-09-09 已实测加了一行
**取反**（代码注释在案），但跳跃路径 `_balance_only_control` 漏掉了同一
取反 → 偏航"阻尼器"实际是偏航"加速器"（回路增益 ~40）：下蹲阶段任何
微打滑踢扰 0.1 s 内放大成 180°/s 螺旋。第 1 跳干净纯属运气（那一跳下蹲
没踢扰）。`test_turn` 的判据只查 ±指令净转角≈0，不查方向，所以漏检。

### 15.2 修复

| 修复 | 位置 |
|---|---|
| 跳跃相位 yaw 修正取反（负反馈） | `combined._balance_only_control`；5D 分支同步取反（当前未启用，防再犯） |
| 触发门槛轮子门改"打滑量" | `launch_mujoco`：轮面速度与机身前向速度差 <0.3 m/s（原来按绝对转速 1.5 rad/s 会禁止行驶中跳跃） |
| 模型 `impratio=10` | `mjcf_builder`：摩擦约束阻抗 ×10，触地冲击 compliance 阶段的微打滑减半 |

### 15.3 修复后实测（与开源对照车同口径）

| 场景 | 本机（修复后） | 开源对照车 |
|---|---|---|
| 原地单跳过程 yaw 峰 | **1.61°** | 0.38° |
| 原地双跳（各跳峰值） | 1.61° / 1.62°，净偏 −0.23° | — |
| 0.4 m/s 行驶中跳 | **0.47°**，落地末速 0.40 m/s（速度保持） | — |
| 行驶中双跳 | 0.47° / 0.36°，净偏 −0.05° | — |

回归：`test_jump` PASS（落地 pitch 8.1°→**5.8°**，弹道 58.6 mm）、
`test_slope_v2` / `test_stand_wheel_balance` / `test_turn` / `test_drive` /
`test_composite` 全部与修复前一致。

落地后的航向锚恢复保留为兜底：净偏≈0 后它无事可做、不再有可见的
"回正动作"。

### 15.4 修复过程中的一个闪退（用户实测 16:44 会话）

打滑量门槛引入了 `mujoco.mj_name2id` 引用，而 `launch_mujoco.py` 的 mujoco
全部是函数内延迟导入、模块级无此名字 → 手柄 A 键上升沿进入门槛即
NameError，进程闪退（telemetry 在 t=25.05 s 戛然而止、system.log 无异常）。
修复：门槛函数内补 `import mujoco`（遵循本文件延迟导入惯例）。已用手柄
链路复现验证（XboxState(A) → mapper → 上升沿 → 门槛 → 起跳全程无崩溃，
按住不重复触发）。

### 15.5 行驶中跳跃不减速（用户反馈第二轮，2026-09-13 晚）

用户实测：行驶中跳会"刹车→起跳→落地车速 0→重新加速"。遥测级排查定位：

1. **跳跃相位 LQR 目标=纯平衡角**：行驶推进靠 STAND 速度环的前倾角，
   跳跃一开始目标被扶正 → 主动刹车。修复：起跳瞬间锁存开环前倾角与
   平衡俯仰角（`_jump_pitch_lean/_jump_eq_pitch`），跳跃全程冻结使用
   （空中逐步重算的平衡角在 0~5° 摆动，LQR 追着摆动目标把车拽减速）。
2. **速度积分被清**：跳跃进入与落地回 STAND 都清速度积分 → 落地后推进
   PI 从零重建、以 ~4.5° 大前倾猛加速追速度（过冲 0.45/指令 0.40）。
   修复：跳跃跨越不清积分（冻结），仅 FALLEN 恢复时清零。
3. **LAND 时长 0.30→0.20**：LAND 期间机体俯仰下潜 + 偏置站姿律水平拖拽
   轮子 → 滑动摩擦刹车（质心速度实测 0.37→0.06 m/s、可短暂倒退），拖拽
   时间 ∝ land_duration；0.20 s 使拖拽减半且原地跳质量略升。
4. **否决的两条路**（机制保留、默认关闭）：
   - 轮速保持（`jump_wheel_speed_hold_gain`，默认 0）：与 LQR 俯仰调节
     抢轮子，0.4 m/s 跳最低质心速度反而 +0.06→−0.05 m/s；
   - 轮前馈（`jump_wheel_ff_scale`，默认 0）：标定假设"俯仰反作用 ∝ 四腿
     力矩共模"对串联腿不成立（髋反作用直接作用机身、膝反作用经大腿传递
     且符号相反），扫 0/±1/±2 倍率速度曲线逐位相同。
5. 触发门槛轮子门改打滑量后，行驶（0.4/0.8 m/s）中跳跃已可触发。

修复后（无头，0.4 m/s）：跳跃全程质心速度 0.40→最低 0.16→0.37 m/s
（恢复 ~0.5 s，无重新加速爬坡）；0.8 m/s：0.80→0.34→0.74。yaw ±0.04°。
原地跳/坡道/站立/转向/行驶/综合全量回归通过。

**残余**：触地瞬间仍有 ~60% 的短暂速度下潜（~0.3 s）。根因是落地冲击使
机体俯仰下潜，偏置站姿律（轮在髋后 0.075 m）把俯仰角速度转化为轮子水平
拖拽，滑动摩擦刹车——17 kg 使该效应比 2.2 kg 的开源车放大 ~8 倍。已有
两次控制器侧尝试（轮速保持/共模前馈）实测无效；进一步消除需要任务空间
落地控制（腿力显式分解水平/垂直）或结构级手段（弹性储能腿/几何重设计），
属下一个专项。

## 16. 跳跃高度提升 + 实机电机规格（2026-09-13 夜）

用户提供实机规格：**膝电机（003/006）峰值 ±60 N·m，髋电机（002/005）峰值
±90 N·m**（此前归档里的 ±40 是探索值）。用户选择把跳跃高度提到 ~12 cm。

### 16.1 改动

| 项 | 原值 | 新值 |
|---|---|---|
| 腿执行器 ctrlrange | 四腿统一 ±40（探索值） | **膝 ±60 / 髋 ±90**（实机规格，`mjcf_builder.LEG_MOTOR_CTRLRANGE`） |
| `air_height_max` | 0.15 | **0.30** |
| `land_torque_limit` | 40 | **60**（= 新满量程，逻辑不变） |
| `test_jump` 力矩门槛 | 40 | 60 |

### 16.2 实测（幅度 1.0）

| 量 | 0.15 时代 | **0.30 现在** |
|---|---|---|
| 实测质心弹道 | 58 mm | **119.8 mm（~12 cm）** |
| EXTEND 膝力矩峰 | 36.4 / ±40（91%） | 59.4 / ±60（**99%**，即实机峰值） |
| EXTEND 髋力矩峰 | — | 52.2 / ±90（58%） |
| 触地速度 | 1.09 m/s | 1.51 m/s |
| 落地恢复 | 0.44 s | 0.40 s（无弹跳、不摔） |
| 飞行 pitch | ~8° | 14.9°（蹬伸反作用随高度放大） |

注意：满幅跳跃时膝电机运行在实机峰值扭矩的 99%——峰值扭矩本就允许短时
（0.19 s）输出，但**连续/高频跳跃建议降幅度**（amp 0.9 ≈ 膝 90%）。

### 16.3 行驶中跳的耦合代价

落地冲击随高度放大 → 偏置站姿律的轮子拖拽 → 滑动摩擦刹车随之加重：
0.4 m/s 行驶中跳 12 cm 时质心速度 0.40→0.01→0.45 m/s（58 mm 时代最低
0.16），恢复 ~0.6 s，偏航仍干净（±0.06°）。"行驶中跳不减速"与"跳跃高度"
在该构型下此消彼长，彻底解决需任务空间落地控制专项（见 15.5 残余）。

全量回归（坡道/站立/转向/行驶/综合/波浪路）全部通过。

## 17. 空中姿态根本修复：镜像髋轴的世界系力矩抵消（2026-09-13 深夜）

用户复测（录屏 19-51，12 cm 跳）：空中机身明显倾斜、腿"不对称"。

### 17.1 遥测结论

5 连跳逐跳分析：**腿实际对称**（左右腿力矩峰 111.5/111.7 N·m，瞬时差
6.9 N·m ≈6%）——"不对称"是 **12~13° 飞行俯仰**的视觉效果。真正的缺陷
是俯仰：蹬伸髋反作用注入起跳俯仰角速度 ~0.65 rad/s，空中积成 12~15°。

### 17.2 根因：飞行姿态阻尼器从未生效过

FLIGHT 阻尼器给两个髋电机施加**同号**关节力矩（`−kd·pitch_rate` 同值写入
两侧髋）。本机左右髋关节轴镜像（左 −X / 右 +X），同一物理扭转在关节坐标
里反号——**同号关节力矩的世界系力矩互相抵消，只摆腿、不俯仰机身**。这就
是从项目早期至今调 `flight_pitch_kd` 始终无效的原因（当时误归因于
"腿摆动惯量小"）。

同一机理也解释了此前两项失败的实验：
- 四腿力矩平均的轮前馈恒≈0（髋负膝正互消）→ 前馈无效；
- 髋"平均"也≈0（左右髋镜像反号 +9.3/−9.3）→ 二次无效。

**凡涉及左右腿的"共模/平均"计算，必须先考虑关节轴镜像。** 正确的驱动量：
- 俯仰类：髋关节坐标**之差** τ_L−τ_R（世界系反作用同向叠加）；
- 竖直推力类：髋关节坐标**之和**（±∂h/∂q 同号）。

### 17.3 修复：反号镜像髋阻尼器

FLIGHT 分支改为右髋(+X 轴)取 +τ、左髋(−X 轴)取 −τ（世界系合力 = 2τ，
腿对作为配重被反向摆动）。符号经实测确定（取反 = 正反馈，飞行 pitch
52.8°）。同时 `flight_pitch_kd` 的 clip 上限 3.5→6.0 N·m。

### 17.4 实测（12 cm 跳，幅度 1.0）

| 量 | 修复前 | 修复后 |
|---|---|---|
| 飞行 pitch 峰 | 12.4~14.9° | **4.7°**（原地）/ 1.9~3.2°（行驶中） |
| 落地后 pitch 峰 | 6.4~12.3° | **2.4°** |
| 每跳 yaw 漂移 | +5.0°（落地倾斜摩擦所致） | **0.0°** |
| 弹道 | 119.8 mm | 119.8 mm（不变） |
| 三连跳累积 | — | 无（每跳独立干净） |

EXTEND 轮前馈（`jump_wheel_ff_scale`）保留机制、默认 0：镜像阻尼器修好后
它反而把倾斜转移到落地（ff=−0.04：飞行 5.7→1.7° 但落地 2.4→12.5°）。

### 17.5 回归

跳跃验收 PASS（弹道 119.8 mm 不变、落地 pitch 2.4°）；坡道/站立/转向/
行驶/综合/波浪路全部与修复前一致；round-trip 一致。
