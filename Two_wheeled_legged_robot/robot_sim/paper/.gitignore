# XML/网格缓存（可重建）
.cache/

# 逐步遥测 CSV（每个场景几 MB，一条命令 2 分钟可重建）。
# 用 ** 覆盖 data/ 下的子目录（例如 data/单腿变高度/），否则 15 MB 的逐步数据会进仓库。
data/**/*.csv

# 例外：论文表格直接引用的汇总表，体积小且是数字出处，保留。
!data/**/roll_3h_metrics.csv

# 汇总指标 JSON 与 index.json 保留，是论文数字的出处（上一行规则之外不做限制）。
